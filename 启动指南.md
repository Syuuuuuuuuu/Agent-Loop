# 项目启动指南（Windows / PowerShell）

> 本项目是一个基于 FastAPI 的 Python Web 应用。启动后在浏览器打开一个网页即可使用。
> 数据库（SQLite）会在第一次启动时自动创建，无需手动初始化。

---

## 零、前置条件（只需确认一次）

1. 已安装 **Python 3.10 及以上**。在 PowerShell 里验证：

```powershell
python --version
```

   若提示找不到命令，去 https://www.python.org/downloads/ 安装，安装时勾选 **Add Python to PATH**。

---

## 一、首次启动（第一次拉到项目时做一遍）

> 全程在 PowerShell 里执行。`cd` 到项目目录后，依次运行下面 3 条。

```powershell
# 1. 进入项目目录
cd "<项目目录>"

# 2. 创建并激活虚拟环境（把依赖装在项目里，不污染系统）
python -m venv .venv
.\.venv\Scripts\Activate.ps1

# 3. 安装依赖
pip install -r requirements.txt
```

> ⚠️ 如果第 2 步激活时报「禁止运行脚本」，先执行一次下面的命令解除限制，再重新激活：
>
> ```powershell
> Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
> ```

装完依赖后，直接接着看「二、启动」。

---

## 二、启动（以后每次都只做这一步）

```powershell
# 1. 进入项目目录
cd "<项目目录>"

# 2. 激活虚拟环境（命令行开头出现 (.venv) 就代表成功）
.\.venv\Scripts\Activate.ps1

# 3. 启动
python run.py
```

看到类似 `Uvicorn running on http://127.0.0.1:8000` 就说明启动成功。

**浏览器打开：** http://127.0.0.1:8000

**关闭服务：** 回到 PowerShell 窗口按 `Ctrl + C`。

---

## 三、配置说明（`.env` 文件）

项目根目录的 `.env` 决定用哪种大模型：

| 配置项 | 说明 |
| --- | --- |
| `LLM_PROVIDER=ark` | 用真实豆包（火山引擎 ARK），需要联网 + 有效 `ARK_API_KEY` |
| `LLM_PROVIDER=mock` | 用离线桩，**不联网、可重复演示**，改错时最省心 |

- 想脱网演示或验证功能：把 `.env` 里的 `LLM_PROVIDER` 改成 `mock`。
- 想用真实模型：保持 `ark`，并确认 `ARK_API_KEY` / `ARK_CHAT_MODEL` 有效。

> 改完 `.env` 需要 **重启服务**（`Ctrl + C` 后重新 `python run.py`）才生效。

---

## 四、验证是否正常（可选）

服务启动后，**另开一个 PowerShell 窗口**跑冒烟测试：

```powershell
cd "<项目目录>"
.\.venv\Scripts\Activate.ps1
python _smoke_test.py
```

看到 `ALL PASSED` 即代表核心链路正常。

---

## 五、常见问题

**Q1：端口 8000 被占用 / 提示 address already in use**
说明已经有一个实例在跑。要么直接用现有的，要么找到占用进程结束它：

```powershell
netstat -ano | findstr :8000
taskkill /PID <上一步查到的PID> /F
```

**Q2：`pip install` 很慢或失败**
换国内镜像重装：

```powershell
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**Q3：调真实模型报鉴权 / 超时错误**
先把 `.env` 改成 `LLM_PROVIDER=mock` 验证程序本身没问题，再回头排查 `ARK_API_KEY` 是否有效、网络是否可达火山引擎。

**Q4：想清空数据（工单、知识库、教学样本）重新演示**
关闭服务后删除项目根目录的 `data.db`，下次启动会自动重建空库：

```powershell
Remove-Item .\data.db
```

---

## 一句话速查

> 日常启动 = `cd 项目目录` → `.\.venv\Scripts\Activate.ps1` → `python run.py` → 浏览器开 http://127.0.0.1:8000
